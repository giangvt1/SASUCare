server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  application:
    name: SWP391_GROUP6
  datasource:
    url: "jdbc:mysql://localhost:3306/test"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true